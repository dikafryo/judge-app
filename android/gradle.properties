org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# QR 스캔(mobile_scanner)의 인식 모델을 APK 에 넣지 않고 Google Play 서비스에서 받아 쓴다.
# 넣으면 APK 가 18MB 가량 커진다 — 앱 크기를 줄이자는 요구와 정면으로 부딪힌다.
# 대가: 모델이 아직 안 받아진 상태에서 완전 오프라인이면 QR 스캔이 안 된다.
# 그때도 접속 코드 직접 입력으로 입장할 수 있고, 아래 매니페스트 meta-data 로
# 설치 직후 모델을 미리 받아 두게 해 두었다.
dev.steenbakker.mobile_scanner.useUnbundled=true
